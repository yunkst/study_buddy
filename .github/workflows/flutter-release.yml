name: Flutter Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: '发布 tag（如 v1.0.0），需已存在'
        required: true
        type: string

permissions:
  contents: write

# 避免同 tag 并发发布互相覆盖
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # 质量门禁：先跑 analyze + test（engine + app），通过后才允许 build APK。
  # 让 quality 必过才能 build，避免任何 analyze/test 报错被掩盖到二进制里。
  quality:
    name: Quality gate (engine + app)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2.18.0
        with:
          flutter-version: '3.x'
          channel: 'stable'
          cache: true

      # --- study_engine（纯 Dart 包）---
      - name: Engine - Install dependencies
        working-directory: packages/study_engine
        run: flutter pub get

      - name: Engine - Analyze
        working-directory: packages/study_engine
        run: dart analyze --no-fatal-infos

      - name: Engine - Run tests
        working-directory: packages/study_engine
        run: |
          rm -rf .dart_tool/sqflite_common_ffi 2>/dev/null || true
          dart test -j 1

      # --- study_buddy（Flutter 应用）---
      - name: App - Install dependencies
        working-directory: study_buddy
        run: flutter pub get

      - name: App - Analyze
        working-directory: study_buddy
        run: flutter analyze --no-fatal-infos

      - name: App - Run tests
        working-directory: study_buddy
        run: |
          rm -rf .dart_tool/sqflite_common_ffi 2>/dev/null || true
          flutter test --no-pub -j 1

  build-and-release:
    name: Build APK & create Release
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        working-directory: study_buddy
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 生成 changelog 需要完整历史

      - name: Resolve tag
        id: tag
        run: |
          if [ -n "${{ inputs.tag }}" ]; then
            TAG="${{ inputs.tag }}"
          else
            TAG=${GITHUB_REF#refs/tags/}
          fi
          echo "name=${TAG}" >> $GITHUB_OUTPUT
          # 不带 v 前缀的纯版本号
          echo "version=${TAG#v}" >> $GITHUB_OUTPUT

      - name: Extract version code from pubspec
        id: version
        run: |
          # pubspec.yaml: version: 1.0.0+1
          # NOTE: defaults.working-directory = study_buddy
          VERSION_CODE=$(grep -E "^version:" pubspec.yaml | sed -E 's/.*\+([0-9]+).*/\1/')
          if [ -z "$VERSION_CODE" ]; then
            echo "❌ 无法从 pubspec.yaml 解析 version code"
            exit 1
          fi
          echo "code=${VERSION_CODE}" >> $GITHUB_OUTPUT

      - name: Detect prerelease
        id: prerelease
        run: |
          # 所有含 '-' 的 tag（如 v1.0.0-preview.1）标记为 prerelease（预览通道），
          # 纯语义化版本 tag（如 v1.0.0）标记为稳定版。
          if [[ "${{ steps.tag.outputs.name }}" == *-* ]]; then
            echo "flag=true" >> $GITHUB_OUTPUT
          else
            echo "flag=false" >> $GITHUB_OUTPUT
          fi

      - name: Extract changelog from tag message
        id: changelog
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 从 tag 的 annotation message 提取 changelog
          # GitHub Actions checkout 会将 annotated tag 退化为 lightweight tag，
          # 导致 %(body)/%(contents)/cat-file 等本地 git 命令均无法获取 message。
          # 改用 GitHub API 直接读取 tag object，最可靠。
          TAG_NAME="${{ steps.tag.outputs.name }}"
          REPO="${{ github.repository }}"

          # 通过 GitHub API 获取 tag object 的 message
          TAG_DATA=$(gh api "repos/$REPO/git/refs/tags/$TAG_NAME" --jq '.object.sha' 2>/dev/null)
          if [ -z "$TAG_DATA" ]; then
            echo "  ⚠️  无法通过 API 获取 tag ref: $TAG_NAME"
            CHANGELOG="Bug 修复和性能优化"
          else
            TAG_TYPE=$(gh api "repos/$REPO/git/tags/$TAG_DATA" --jq '.message // empty' 2>/dev/null)
            if [ -z "$TAG_TYPE" ]; then
              # ref 指向的可能不是 tag object 而是 commit (lightweight)
              # 尝试直接从 ref 的 object 获取
              OBJECT_URL=$(gh api "repos/$REPO/git/refs/tags/$TAG_NAME" --jq '.object.url' 2>/dev/null)
              TAG_MSG=$(gh api "$OBJECT_URL" --jq '.message // .tag.message // empty' 2>/dev/null)
              if [ -z "$TAG_MSG" ]; then
                CHANGELOG="Bug 修复和性能优化"
              else
                # 跳过第一行 "Release X.Y.Z" 和空行
                CHANGELOG=$(echo "$TAG_MSG" | tail -n +3)
                if [ -z "$(echo "$CHANGELOG" | tr -d '[:space:]')" ]; then
                  CHANGELOG="Bug 修复和性能优化"
                fi
              fi
            else
              # tag object 有 message 字段
              CHANGELOG=$(echo "$TAG_TYPE" | tail -n +3)
              if [ -z "$(echo "$CHANGELOG" | tr -d '[:space:]')" ]; then
                CHANGELOG="Bug 修复和性能优化"
              fi
            fi
          fi
          {
            echo "changelog<<EOF"
            echo "$CHANGELOG"
            echo "EOF"
          } >> $GITHUB_OUTPUT
          echo "===== 提取到的 changelog ====="
          echo "$CHANGELOG"
          echo "=============================="

      - name: Render release body
        env:
          VERSION: ${{ steps.tag.outputs.version }}
          CHANGELOG: ${{ steps.changelog.outputs.changelog }}
        run: |
          # 用实际版本号替换 {{VERSION}}，用 changelog 替换 {{CHANGELOG}}
          # 用 Python 避免 sed 的特殊字符（/、&、\、换行）转义问题
          # NOTE: defaults.working-directory = study_buddy，所以 ../.github = 仓库根
          python3 -c '
          import os
          with open("../.github/release_template.md", "r", encoding="utf-8") as f:
              body = f.read()
          body = body.replace("{{VERSION}}", "v" + os.environ["VERSION"])
          body = body.replace("{{CHANGELOG}}", os.environ["CHANGELOG"])
          with open("release_body.md", "w", encoding="utf-8") as f:
              f.write(body)
          '
          echo "===== 渲染后的 release_body.md ====="
          cat release_body.md

      - name: Setup Flutter
        uses: subosito/flutter-action@v2.18.0
        with:
          flutter-version: '3.x'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Decode release keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
        run: |
          set -e
          if [ -z "$ANDROID_KEYSTORE_BASE64" ]; then
            echo "❌ 缺少 secret ANDROID_KEYSTORE_BASE64，请在仓库 Settings → Secrets 配置"
            echo "   （首次发布前需配置 Android release 签名 keystore，详见 .claude/skills/study-app-release/SKILL.md）"
            exit 1
          fi
          # keystore 放在 android 根目录（rootProject），key.properties 中的 storeFile 用相对路径解析
          echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > android/study-buddy-release-key.jks
          cat > android/key.properties <<EOF
          storePassword=$ANDROID_KEYSTORE_PASSWORD
          keyPassword=$ANDROID_KEYSTORE_PASSWORD
          keyAlias=$ANDROID_KEY_ALIAS
          storeFile=study-buddy-release-key.jks
          EOF
          echo "✅ keystore 解码完成，key.properties 已生成"
          ls -la android/study-buddy-release-key.jks

      - name: Build per-architecture release APKs
        run: flutter build apk --release --split-per-abi

      - name: Compute SHA256 checksums
        working-directory: study_buddy/build/app/outputs/flutter-apk
        run: |
          sha256sum app-*-release.apk > SHA256SUMS.txt
          echo "===== SHA256SUMS.txt ====="
          cat SHA256SUMS.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          name: ${{ steps.tag.outputs.name }}
          body_path: study_buddy/release_body.md
          # draft 永远 false：preview 版也直接发布（仅靠 prerelease 标记区分通道），
          # 否则 /releases?per_page=1 拿不到 draft 版本，预览通道失效。
          draft: false
          prerelease: ${{ steps.prerelease.outputs.flag == 'true' }}
          files: |
            study_buddy/build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
            study_buddy/build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
            study_buddy/build/app/outputs/flutter-apk/app-x86_64-release.apk
            study_buddy/build/app/outputs/flutter-apk/SHA256SUMS.txt
