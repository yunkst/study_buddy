# Dart / Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock: 应用包应提交 lock 以保证 CI/本地一致；纯 Dart 包(package)则忽略。
# 此处忽略根级与 packages/ 下的 lock，保留 study_buddy/ 应用包的 lock（见 study_buddy/.gitignore 未忽略）。
/pubspec.lock
/packages/**/pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Claude / Superpowers 工作区（本地 scratch，不入库）
.superpowers/
.claude/worktrees/
.worktrees/
.agents/

# 本地代码审查 diff 草稿
review_diff*.txt

# MCP 调试产物（Playwright 等本地探针输出，不入库）
.playwright-mcp/

# 本地一次性预览/变更日志草稿
preview-fullpage.png
.changelog_preview*.txt

# 内部开发规范与历史过程文档（不随公开仓库发布）
docs/superpowers/
docs/logging-guidelines.md

# 发布脚本临时文件
TAG_EDITMSG_TMP
__pycache__/
*.pyc

# Android release 签名(绝不入库)
*.jks
*.keystore
key.properties

# 本地生成的一次性密码/临时文件(发布配置时由 Claude 写入,任务结束自删)
.jks_pass.tmp

# 根级实验/脚本产物（非 study_buddy 工程文件，勿入库）
# 包括题库抓取实验、node 脚本、运行时 JSON 缓存等
node_modules/
package.json
package-lock.json
keyky_*.js
.h5_index.js
.html
.err
.r.json
.q.json
.qd.json
.qblist.json
.member.json
# app 图标生成脚本与候选设计稿（一次性素材，正式图标已落 study_buddy/assets/）
gen_icons.py
icon_options/

# 根级设计预览稿（HTML 探索素材，非 app 工程文件，勿入库）
design-preview/

# 推广物料（掘金文章/小红书封面等本地草稿，勿入库）
promotion/
