import '../db/database.dart';

/// 批改记录仓库。Task 3 填充 save/findById/findBySession。
class ReviewRepository {
  // ignore: unused_field —— 本 Task 仅注入占位，Task 3 实现方法时使用。
  final StudyDatabase _db;
  ReviewRepository(this._db);
}
